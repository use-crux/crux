---
"@use-crux/core": minor
---

Add request-scoped `defer(callback)` with bounded host-lifetime execution and the explicit `@use-crux/core/defer/node` HTTP integration for response-finished cleanup and graceful shutdown.
