# Superseded TUI Plan

This file is intentionally retained as a pointer for older references.

The original V1 Panels redesign plan has been superseded by the TUI/CLI overhaul specs and the
implementation now present under `internal/theme`, `internal/tui/kit`, `internal/tui/bridge`,
`internal/tui/shell`, `internal/tui/screens`, and `internal/tui/overlays`.

Do not use the old plan as an implementation source. Current architecture rules live in
`ARCHITECTURE.md`, and remaining action-surface follow-ups are tracked in the local overhaul kit
progress file.
