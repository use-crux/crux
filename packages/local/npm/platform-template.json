{
  "name": "@use-crux/local-${PLATFORM}-${ARCH}",
  "version": "0.1.0",
  "description": "Crux local runtime binary for ${PLATFORM}-${ARCH}",
  "os": ["${PLATFORM}"],
  "cpu": ["${ARCH}"],
  "files": ["bin"],
  "license": "Apache-2.0"
}
