// Package tui — view_dashboard.go has been retired.
//
// The legacy trace-tree dashboard was replaced by the V1 Panels Quality
// Workbench (see workbench.go + screens/*). Trace-list functionality now
// lives behind the `traces` top tab, which is currently a stub awaiting its
// own redesign pass.
//
// File intentionally left as a marker so git history is preserved for the
// migration commit. Safe to remove in a follow-up.
package tui
